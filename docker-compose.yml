services:

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: tradingjournal
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      retries: 5

  api:
    image: jovityl/trading-journal-api:latest
    ports:
      - "8080:8080"
    environment:
      - ConnectionStrings__DefaultConnection=Host=db;Database=tradingjournal;Username=postgres;Password=postgres
      - Auth0__Domain=${AUTH0_DOMAIN}
      - Auth0__Audience=${AUTH0_AUDIENCE}
      - Anthropic__ApiKey=${ANTHROPIC_API_KEY}
      - OpenRouter__ApiKey=${OPENROUTER_API_KEY}
      - Admin__Emails__0=${ADMIN_EMAIL}
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads

  frontend:
    image: jovityl/trading-journal-frontend:latest
    ports:
      - "3000:80"
    depends_on:
      - api

volumes:
  pgdata:
  uploads:
